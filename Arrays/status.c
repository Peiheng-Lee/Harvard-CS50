#include <cs50.h>
#include <stdio.h>
#include <string.h>

int main(int argc; string argv[])
{
    if (agrc != 2)
    {
        printf("Missing command-line arguments.\n");
        return 1;
    }
    printf("hello, %s\n", argv[1])
    else
    {
        return 0;
    }
}
