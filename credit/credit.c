#include <cs50.h>
#include <stdio.h>
int main(void)
{
    long number;
    do
    {
        number = get_long("Number:");
    }
    while (number < 1);

}

int luhn_isvalid(long n)
{
    for (i = strlen(n) - 1; i >= 0; i--)
    {
        if 
    }
}
