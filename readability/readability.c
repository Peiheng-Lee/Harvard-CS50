#include <ctype.h>
#include <cs50.h>
#include <math.h>
#include <stdio.h>
#include <string.h>

int main(void)
{
    string text = get_string("Text:");
    count_letters()
}

int count_letters(string text)
{
    for (i = 0, len = strlen(text); i < len; i++)
    {
        if text[i] != 
    }
}
